# Sample

This folder contains [`themes.css`](themes.css), the Signal Desktop stylesheet for Dracula.

Use the steps in [`INSTALL.md`](../INSTALL.md) to inject it into Signal’s `app.asar` (or use the Nix flake in the repository root).
